diff --git a/core/scripts/context-host.ps1 b/core/scripts/context-host.ps1
index bbae36f..131f66e 100644
--- a/core/scripts/context-host.ps1
+++ b/core/scripts/context-host.ps1
@@ -288,7 +288,15 @@ namespace CapturePack {
             learned.Top = dwm.Top - raw.Top;
             learned.Right = dwm.Right - raw.Right;
             learned.Bottom = dwm.Bottom - raw.Bottom;
-            Inset[key] = learned;
+            // Raw being unchanged proves only that GetWindowRect stood still.
+            // DWM can still be publishing the tail of the preceding drag; in
+            // that case the difference is travel, not a resize border. A real
+            // invisible border can only inset the visible frame inside raw.
+            bool validInset = learned.Left >= 0 && learned.Top >= 0 &&
+              learned.Right <= 0 && learned.Bottom <= 0 &&
+              raw.Left + learned.Left < raw.Right + learned.Right &&
+              raw.Top + learned.Top < raw.Bottom + learned.Bottom;
+            if (validInset) Inset[key] = learned;
           }
           LastRaw[key] = raw;
           RECT known;
@@ -651,7 +659,13 @@ while ($running) {
         # round trip is still measurable rather than hidden.
         $line = [CapturePack.SurfaceLane]::Sample((Get-HostMs))
         if ($null -ne $frameMs) {
-          $line = $line.Insert(1, '"ft":' + [Math]::Round($frameMs, 3) + ',')
+          # THIS NUMBER IS ALSO THE CORRELATION KEY. Core keys the outstanding
+          # request by the exact JavaScript double it sent; rounding the echo to
+          # three decimals makes a normal fractional performance timestamp a
+          # different key, so the reply loses its age/delay and never settles a
+          # held sample. "R" is the invariant round-trip representation.
+          $frameText = $frameMs.ToString('R', [System.Globalization.CultureInfo]::InvariantCulture)
+          $line = $line.Insert(1, '"ft":' + $frameText + ',')
         }
         Write-Line $line
         Write-Line ('{"id":' + $id + ',"ok":true}')
diff --git a/core/scripts/surface-sync-check.mjs b/core/scripts/surface-sync-check.mjs
index 330c1db..13ce433 100644
--- a/core/scripts/surface-sync-check.mjs
+++ b/core/scripts/surface-sync-check.mjs
@@ -1,37 +1,69 @@
 // Runner for surface-sync-check.ts (#110). The check imports main-process
-// TypeScript, so it is bundled first — with `electron` stubbed, because the only
-// thing the import chain wants from it is a log directory.
+// TypeScript, so its dependency graph is emitted into one disposable CommonJS
+// tree — with `electron` stubbed, because the only thing the import chain wants
+// from it is a log directory.
 import { execFileSync } from 'node:child_process'
-import { mkdtempSync, writeFileSync, rmSync } from 'node:fs'
-import { tmpdir } from 'node:os'
+import { mkdirSync, mkdtempSync, writeFileSync, rmSync } from 'node:fs'
 import path from 'node:path'
 import { fileURLToPath } from 'node:url'
+import ts from 'typescript'
 
 const here = path.dirname(fileURLToPath(import.meta.url))
-const work = mkdtempSync(path.join(tmpdir(), 'capturepack-sync-'))
+const core = path.join(here, '..')
+// node_modules is already required for this runner and ignored by git. Keeping
+// the disposable emit there also keeps the whole check inside its worktree —
+// important in managed environments where the OS temp directory is outside
+// the allowed filesystem boundary.
+const work = mkdtempSync(path.join(core, 'node_modules', '.capturepack-sync-'))
 try {
-  const stub = path.join(work, 'electron-stub.cjs')
+  const stubDir = path.join(work, 'node_modules', 'electron')
+  mkdirSync(stubDir, { recursive: true })
   writeFileSync(
-    stub,
+    path.join(stubDir, 'index.js'),
     `const p=require('node:path');exports.app={getPath:()=>p.join(${JSON.stringify(work)},'logs'),` +
       `getAppPath:()=>'.',isPackaged:false,getVersion:()=>'0.0.0-check',on:()=>{},` +
       `whenReady:()=>Promise.resolve()};\n`,
   )
-  const bundle = path.join(work, 'check.cjs')
-  execFileSync(
-    process.execPath,
-    [
-      path.join(here, '..', 'node_modules', 'esbuild', 'bin', 'esbuild'),
-      path.join(here, 'surface-sync-check.ts'),
-      '--bundle',
-      '--platform=node',
-      '--format=cjs',
-      `--outfile=${bundle}`,
-      `--alias:electron=${stub}`,
-    ],
-    { stdio: ['ignore', 'ignore', 'inherit'] },
-  )
-  execFileSync(process.execPath, [bundle], { stdio: 'inherit' })
+
+  // Use the already-required TypeScript compiler rather than esbuild's native
+  // helper. Managed Windows sandboxes can deny that child binary access to the
+  // worktree even though this Node process can read it, which made the check
+  // fail before its first assertion. This is the same typed import graph that
+  // `npx tsc --noEmit` validates, emitted only so Node can execute the harness.
+  const configPath = path.join(core, 'tsconfig.json')
+  const config = ts.readConfigFile(configPath, ts.sys.readFile)
+  const parsed = ts.parseJsonConfigFileContent(config.config ?? {}, ts.sys, core)
+  const configDiagnostics = [config.error, ...parsed.errors].filter(Boolean)
+  const diagnosticHost = {
+    getCanonicalFileName: (file) => file,
+    getCurrentDirectory: () => core,
+    getNewLine: () => '\n',
+  }
+  if (configDiagnostics.length > 0) {
+    throw new Error(ts.formatDiagnosticsWithColorAndContext(configDiagnostics, diagnosticHost))
+  }
+  const program = ts.createProgram([path.join(here, 'surface-sync-check.ts')], {
+    ...parsed.options,
+    noEmit: false,
+    declaration: false,
+    declarationMap: false,
+    sourceMap: false,
+    incremental: false,
+    composite: false,
+    module: ts.ModuleKind.CommonJS,
+    moduleResolution: ts.ModuleResolutionKind.Node10,
+    rootDir: core,
+    outDir: work,
+  })
+  const diagnostics = ts.getPreEmitDiagnostics(program)
+  if (diagnostics.length > 0) {
+    throw new Error(ts.formatDiagnosticsWithColorAndContext(diagnostics, diagnosticHost))
+  }
+  const emitted = program.emit()
+  if (emitted.emitSkipped) throw new Error('surface-sync-check TypeScript emit was skipped')
+  execFileSync(process.execPath, [path.join(work, 'scripts', 'surface-sync-check.js')], {
+    stdio: 'inherit',
+  })
 } finally {
   rmSync(work, { recursive: true, force: true })
 }
diff --git a/core/scripts/surface-sync-check.ts b/core/scripts/surface-sync-check.ts
index 85136ba..2fcd0fd 100644
--- a/core/scripts/surface-sync-check.ts
+++ b/core/scripts/surface-sync-check.ts
@@ -17,10 +17,11 @@
 // alternating direction, up to 13,000 px/s. The rectangles were right; the times
 // they were filed under were not.
 //
-// On top of that, `IPC.captureTick` has always been documented "Focused display
-// only — one clock" and nothing enforced it: `capture.ts` registers the tick
-// listener per capture window, so EVERY display's recorder ticked into the one
-// lane, each with its own media clock and its own zero point.
+// On top of that, Lane S still defends its one-clock boundary: `capture.ts`
+// registers a listener per recorder, and a future routing regression must not
+// let a second display interleave its ticks. Production now normalizes every
+// recorder's DOMHighRes timestamp to SessionClock before this boundary, but the
+// lane accepting only its first source remains cheap, independent protection.
 //
 // WHY IT NEEDS A SYNTHETIC HOST. Neither fault is reachable through a real
 // PowerShell host driven on a schedule — they need several ticks in flight with
@@ -32,6 +33,7 @@ import { SurfaceTimeline } from '../src/main/context/timeline'
 import { frozenRingObservations } from '../src/main/context/ringObservations'
 import type { HostMonitor } from '../src/main/context/surfaceLane'
 import type { HostReply } from '../src/main/context/host'
+import type { SurfaceInfo } from '../src/shared/context/protocol'
 
 /** Ground truth: one window sliding at exactly 1 px/ms, plus a static neighbour. */
 function deskAt(tMs: number): unknown[] {
@@ -273,19 +275,54 @@ function report(title: string, r: Result): boolean {
   return ok
 }
 
+/** A fractional last sample that rounds to replay end must not publish twice. */
+function roundedEndLabelIsUnique(): boolean {
+  const monitor: HostMonitor = {
+    device: 'END-BENCH',
+    primary: true,
+    bounds: { x: 0, y: 0, width: 100, height: 100 },
+  }
+  const surface: SurfaceInfo = {
+    surfaceId: 'surface-end',
+    hwnd: '9',
+    bounds: { x: 10, y: 10, width: 20, height: 20 },
+    zOrder: 0,
+    visible: true,
+    minimized: false,
+    foreground: true,
+  }
+  const observations = frozenRingObservations(
+    () => ({ surfaces: [surface] }),
+    [monitor],
+    [{ index: 0, focused: true, width: 100, height: 100 }],
+    100,
+    [99.6],
+  )
+  const atEnd = observations.filter((o) => o.tMs === 100).length
+  const ok = observations.length === 1 && atEnd === 1
+  console.log('C: a fractional final sample rounds onto the replay-end label')
+  console.log(
+    ok
+      ? '  PASS — replay end is published once\n'
+      : `  FAIL — replay end was published ${atEnd} times across ${observations.length} observations\n`,
+  )
+  return ok
+}
+
 async function main(): Promise<void> {
-  // Two recorder renderers have independent media clocks with independent zero
-  // points, so their frame times are thousands of ms apart. Scenario B removes
-  // that so the tick/reply mismatch is measured on its own.
+  // Scenario A gives the ignored display an alien clock to prove that Lane S's
+  // source boundary really excludes it. Scenario B removes that disagreement so
+  // the tick/reply correlation is measured on its own.
   const a = report(
-    'A: two displays with independent media clocks (what production does)',
+    'A: a second display offers an alien frame clock (lane boundary defense)',
     await run(4_321),
   )
   const b = report(
-    'B: two displays whose media clocks happen to agree (tick/reply mismatch alone)',
+    'B: both display clocks agree (tick/reply mismatch alone)',
     await run(0),
   )
-  if (!a || !b) {
+  const c = roundedEndLabelIsUnique()
+  if (!a || !b || !c) {
     console.error('surface-sync-check FAILED')
     process.exitCode = 1
     return
diff --git a/core/scripts/uia-dump.ps1 b/core/scripts/uia-dump.ps1
index 48919d7..0c52de9 100644
--- a/core/scripts/uia-dump.ps1
+++ b/core/scripts/uia-dump.ps1
@@ -72,6 +72,14 @@ function Get-EnvLong([string]$name) {
   return 0L
 }
 
+# UI Automation exposes NativeWindowHandle as a SIGNED 32-bit integer; the
+# surface host serializes the same HWND's low 32 bits as an unsigned decimal.
+# Once bit 31 is set, comparing their ordinary strings makes one window look
+# like two identities and the visible-window filter skips every control in it.
+function ConvertTo-HwndString([int]$value) {
+  return [string]([uint32](([int64]$value) -band 0xffffffffL))
+}
+
 $MaxDepth = Get-EnvInt 'CAPTUREPACK_UIA_MAX_DEPTH' 12
 $MaxElements = Get-EnvInt 'CAPTUREPACK_UIA_MAX_ELEMENTS' 3000
 # Defaults match src/main/uia.ts (which always sets these explicitly); the cap
@@ -292,6 +300,7 @@ foreach ($win in $topLevel) {
     if ($win.Cached.IsOffscreen) { continue }
     $handle = 0
     try { $handle = [int]$win.Cached.NativeWindowHandle } catch { $handle = 0 }
+    $handleKey = ConvertTo-HwndString $handle
     $className = [string]$win.Cached.ClassName
     $title = [string]$win.Cached.Name
     if ([string]::IsNullOrWhiteSpace($title)) { $title = $className }
@@ -303,9 +312,9 @@ foreach ($win in $topLevel) {
     # description that two sources can disagree about: this dump reports the
     # process as "explorer" where the surface host reports "explorer.exe", and
     # an untitled window has its CLASS put in `title` a few lines above. The
-    # handle is the one thing both are looking at. Decimal string, because a
-    # Win64 HWND does not fit a JS number safely.
-    [void]$windowJson.Append('{"hwnd":').Append((ConvertTo-JsonString ([string]$handle)))
+    # handle is the one thing both are looking at. Decimal string, normalized
+    # to the unsigned spelling the surface host uses (see ConvertTo-HwndString).
+    [void]$windowJson.Append('{"hwnd":').Append((ConvertTo-JsonString $handleKey))
     [void]$windowJson.Append(',"title":').Append((ConvertTo-JsonString $title))
     [void]$windowJson.Append(',"process":').Append((ConvertTo-JsonString $processName))
     [void]$windowJson.Append(',"class_name":').Append((ConvertTo-JsonString $className))
@@ -321,7 +330,7 @@ foreach ($win in $topLevel) {
       # test buys more than raising the budget ever could.
       Walkable = (
         (-not ($desktopClasses -contains $className)) -and
-        ($VisibleHandles.Count -eq 0 -or $VisibleHandles.ContainsKey([string]$handle))
+        ($VisibleHandles.Count -eq 0 -or $VisibleHandles.ContainsKey($handleKey))
       )
     })
   } catch { continue }
diff --git a/core/src/main/annotatedRender.ts b/core/src/main/annotatedRender.ts
index 9b6c86d..16b2ab6 100644
--- a/core/src/main/annotatedRender.ts
+++ b/core/src/main/annotatedRender.ts
@@ -163,9 +163,18 @@ export function startAnnotatedRender(
   emitRenderState(handle.dirPath, 'rendering')
   const finish = (state: 'done' | 'failed'): void => {
     const count = (inFlight.get(key) ?? 1) - 1
-    if (count <= 0) inFlight.delete(key)
-    else inFlight.set(key, count)
-    emitRenderState(handle.dirPath, state)
+    if (count <= 0) {
+      inFlight.delete(key)
+      emitRenderState(handle.dirPath, state)
+    } else {
+      inFlight.set(key, count)
+      // This job ended, but the pack did not. A re-edit can queue a replacement
+      // while an older render is running; publishing that older job's terminal
+      // state enables Retry and clears History's progress while the replacement
+      // is still queued. The aggregate lifecycle remains indeterminate until
+      // the last counted job reaches its own terminal state.
+      emitRenderState(handle.dirPath, 'rendering')
+    }
     onDone(state)
   }
   // Progress goes BOTH ways: to the caller that asked for it (the save toast)
diff --git a/core/src/main/capture.ts b/core/src/main/capture.ts
index 968d826..df0aed4 100644
--- a/core/src/main/capture.ts
+++ b/core/src/main/capture.ts
@@ -177,6 +177,10 @@ const probesSinceProof = new Map<number, number>()
 const probesInFlight = new Set<number>()
 let reconcileTimer: ReturnType<typeof setInterval> | undefined
 let wantedDisplayIds = new Set<number>()
+// The one recorder renderer that pays for frame callbacks and drives Lane S.
+// Stable while its display remains wanted, so an ordinary cursor move never
+// creates a second tick source or rebuilds a healthy replay buffer.
+let surfaceTickSourceDisplayId: number | null = null
 let publishedRecorderState: RecorderState = { status: 'starting' }
 const recorderStateListeners = new Set<(state: RecorderState) => void>()
 
@@ -438,17 +442,6 @@ export function recorderCadence(displayId: number): { achievedFps: number; worst
   return displayCadence.get(displayId) ?? null
 }
 
-/**
- * The display whose recording IS the pack clock (SPEC §10.1) — the cursor's.
- *
- * Only it ticks the surface ring (#105): a second display's frames carry
- * another recording's numbers, and filing samples under those would put the
- * ring on two clocks at once, which is the problem rather than the fix.
- */
-function focusedDisplayIdForTicks(): number {
-  return screen.getDisplayNearestPoint(screen.getCursorScreenPoint()).id
-}
-
 function onFramesProven(displayId: number, payload: CaptureFramesPayload): void {
   if (!wantedDisplayIds.has(displayId)) return
   if (payload.cadence !== undefined) displayCadence.set(displayId, payload.cadence)
@@ -567,6 +560,7 @@ export function disposeCapture(): void {
   recoveryAttempts.clear()
   probesSinceProof.clear()
   probesInFlight.clear()
+  surfaceTickSourceDisplayId = null
 }
 
 /**
@@ -851,7 +845,7 @@ export interface ReplayFetch {
     durationMs: number
     mimeType: string
     replayFile: 'replay.webm' | 'replay.mp4'
-    /** The tick clock's value at these bytes' t=0 (#112); absent if unknown. */
+    /** DOMHighRes wall-comparable time of these bytes' t=0 (#112); absent if unknown. */
     originMs?: number
   } | null
   // Set exactly when `replay` is null.
@@ -879,7 +873,7 @@ export function requestReplay(
             durationMs: payload.durationMs,
             mimeType: payload.mimeType,
             replayFile: payload.replayFile,
-            // Where these bytes begin on the tick clock (#112).
+            // Where these bytes begin on the cross-renderer tick axis (#112).
             ...(typeof payload.originMs === 'number' && Number.isFinite(payload.originMs)
               ? { originMs: payload.originMs }
               : {}),
@@ -962,7 +956,11 @@ function queueRebuild(): Promise<void> {
 
 // Everything the running recorder depends on: a window whose signature still
 // matches can be kept across a rebuild, replay buffer intact.
-function recorderSignature(display: Display, settings: Settings): string {
+function recorderSignature(
+  display: Display,
+  settings: Settings,
+  ticksSurfaceLane: boolean,
+): string {
   return [
     display.size.width,
     display.size.height,
@@ -970,6 +968,10 @@ function recorderSignature(display: Display, settings: Settings): string {
     settings.fps,
     settings.replaySeconds,
     settings.replayMaxWidth,
+    // This flag is baked into CaptureStartPayload. If ownership moves because
+    // a monitor disappears (or fixed-display mode changes), an existing
+    // replacement renderer cannot become the tick source without a restart.
+    ticksSurfaceLane ? 1 : 0,
   ].join(':')
 }
 
@@ -991,6 +993,17 @@ async function rebuild(): Promise<void> {
         : [resolveFixedDisplay(settings.captureDisplay)]
   const wanted = new Map<number, Display>(displays.map((d) => [d.id, d]))
   wantedDisplayIds = new Set(wanted.keys())
+  // EXACTLY ONE TICK SOURCE, AND IT HAS TO BE A RECORDER WE ARE CREATING.
+  // Fixed-display mode used to compare its sole display with the CURSOR's id;
+  // when the cursor was elsewhere, the only recorder received `focused:false`
+  // and the frame-anchored path never ran at all. Prefer the cursor only when it
+  // is in this recorder set, otherwise fall back to the set's first display.
+  if (surfaceTickSourceDisplayId === null || !wanted.has(surfaceTickSourceDisplayId)) {
+    const cursorDisplayId = screen.getDisplayNearestPoint(screen.getCursorScreenPoint()).id
+    surfaceTickSourceDisplayId = wanted.has(cursorDisplayId)
+      ? cursorDisplayId
+      : (displays[0]?.id ?? null)
+  }
   for (const id of displayRecorderStates.keys()) {
     if (!wanted.has(id)) displayRecorderStates.delete(id)
   }
@@ -1018,7 +1031,8 @@ async function rebuild(): Promise<void> {
       // restarted. A rebuild is the one moment recorders are allowed to be
       // recreated, so this is where it gets a fresh renderer.
       displayRecorderStates.get(id)?.status === 'stopped' ||
-      captureWindowSigs.get(id) !== recorderSignature(display, settings)
+      captureWindowSigs.get(id) !==
+        recorderSignature(display, settings, id === surfaceTickSourceDisplayId)
     if (!stale) continue
     // destroy() emits 'closed', whose handler releases the window's IPC
     // listener and assignedDisplays entry — no leaks.
@@ -1041,9 +1055,16 @@ async function rebuild(): Promise<void> {
     if (captureWindows.has(display.id)) continue
     setDisplayRecorderState(display.id, { status: 'starting' })
     try {
-      const win = await createCaptureWindow(display, settings)
+      const win = await createCaptureWindow(
+        display,
+        settings,
+        display.id === surfaceTickSourceDisplayId,
+      )
       captureWindows.set(display.id, win)
-      captureWindowSigs.set(display.id, recorderSignature(display, settings))
+      captureWindowSigs.set(
+        display.id,
+        recorderSignature(display, settings, display.id === surfaceTickSourceDisplayId),
+      )
       if (displayRecorderStates.get(display.id)?.status !== 'stopped') {
         scheduleRecorderProbe(display.id, win, RECORDER_STARTUP_PROBE_DELAY_MS)
       }
@@ -1056,7 +1077,11 @@ async function rebuild(): Promise<void> {
   publishRecorderState()
 }
 
-async function createCaptureWindow(display: Display, settings: Settings): Promise<BrowserWindow> {
+async function createCaptureWindow(
+  display: Display,
+  settings: Settings,
+  ticksSurfaceLane: boolean,
+): Promise<BrowserWindow> {
   const win = new BrowserWindow({
     show: false,
     skipTaskbar: true,
@@ -1157,8 +1182,9 @@ async function createCaptureWindow(display: Display, settings: Settings): Promis
     const slowReplayMs = simulateSlowReplayMs()
     const payload: CaptureStartPayload = {
       displayId: String(display.id),
-      // The pack clock's owner is the only display that ticks the ring (#105).
-      focused: display.id === focusedDisplayIdForTicks(),
+      // One recorder drives Lane S; its clock is normalized in runtime.ts, so
+      // it need not be the display a later capture happens to focus (#105).
+      focused: ticksSurfaceLane,
       fps: settings.fps,
       // The recorder rotates segments at this interval; replay covers 1x..2x of it.
       segmentSeconds: settings.replaySeconds,
diff --git a/core/src/main/chrome/domBridge.ts b/core/src/main/chrome/domBridge.ts
index 40fb268..de14ae4 100644
--- a/core/src/main/chrome/domBridge.ts
+++ b/core/src/main/chrome/domBridge.ts
@@ -42,7 +42,7 @@ export interface DomBridgeStatus {
   listening: boolean
   /** A host process has connected at least once this run. */
   hostSeen: boolean
-  /** The extension completed a hello handshake. */
+  /** At least one live host socket completed the extension hello handshake. */
   extensionConnected: boolean
   extensionVersion: string | null
   protocolVersion: number | null
@@ -58,6 +58,9 @@ let hostSeen = false
 let extensionVersion: string | null = null
 let extensionProtocol: number | null = null
 let lastEventAtMs: number | null = null
+// A completed hello is connection state, not run history. Settings uses it to
+// distinguish a host that once reached the app from one that is alive now.
+const handshakenSockets = new Set<net.Socket>()
 /** Supplied by the context runtime: "now" on the replay clock. */
 let clockNowMs: () => number = () => Date.now()
 
@@ -85,7 +88,7 @@ export function domBridgeStatus(): DomBridgeStatus {
   return {
     listening: server !== null,
     hostSeen,
-    extensionConnected: extensionVersion !== null,
+    extensionConnected: handshakenSockets.size > 0,
     extensionVersion,
     protocolVersion: extensionProtocol,
     protocolCompatible: extensionProtocol === DOM_PROTOCOL_VERSION,
@@ -203,6 +206,7 @@ export function startDomBridge(): void {
           }
           const result = parse(parsed)
           if (result === 'hello') {
+            handshakenSockets.add(socket)
             logInfo(
               `[chrome] extension ${extensionVersion ?? 'unknown'} connected, protocol v${String(extensionProtocol)}`,
             )
@@ -219,6 +223,9 @@ export function startDomBridge(): void {
       }
     })
     socket.on('error', () => socket.destroy())
+    socket.on('close', () => {
+      handshakenSockets.delete(socket)
+    })
   })
   srv.on('error', (err) => {
     // EADDRINUSE means another CapturePack owns the channel — the single
@@ -243,6 +250,7 @@ export function stopDomBridge(): void {
   events = []
   extensionVersion = null
   extensionProtocol = null
+  handshakenSockets.clear()
   hostSeen = false
   lastEventAtMs = null
 }
diff --git a/core/src/main/context/ringObservations.ts b/core/src/main/context/ringObservations.ts
index 3676cc2..ec33055 100644
--- a/core/src/main/context/ringObservations.ts
+++ b/core/src/main/context/ringObservations.ts
@@ -286,7 +286,7 @@ export function frozenRingObservations(
   // The capture instant itself, which the walk only lands on when a sample
   // happened to fall exactly there. It is the one moment the user is guaranteed
   // to look at.
-  if (times[times.length - 1] !== end) {
+  if (times[times.length - 1] !== end && previousLabel !== end) {
     const last = surfacesAt(end)
     if (last !== null && last.surfaces.length > 0) {
       observations.push(observationOf(end, last.surfaces, spaces))
diff --git a/core/src/main/context/runtime.ts b/core/src/main/context/runtime.ts
index b3da47e..fd2b108 100644
--- a/core/src/main/context/runtime.ts
+++ b/core/src/main/context/runtime.ts
@@ -145,24 +145,26 @@ export function updateContextRetention(replayMs: number): void {
 export function freezeContext(
   triggerAtWallMs: number,
   replayDurationMs: number,
-  tickOriginMs?: number,
+  replayOriginWallMs?: number,
 ): string | null {
   const current = runtime
   if (current === null) return null
   // THE REPLAY'S OWN ORIGIN WHEN THE RECORDER GAVE ONE (#112).
   //
-  // Ticks carry a session-monotonic `presentationTime`, so the ring is on that
-  // clock. The saved bytes begin where their recorder slot began, and the
-  // recorder reports that number with them — so the frozen range is anchored on
-  // it directly. No wall clock, no conversion, no residual.
+  // Recorder windows do not share a `performance.now()` origin. They report
+  // `performance.timeOrigin + presentationTime`, so ticks and a replay returned
+  // by different displays remain comparable; this is the ONE boundary where
+  // that DOMHighRes wall-comparable value becomes SessionClock. The saved bytes
+  // begin where their recorder slot began, and the recorder reports that number
+  // with them.
   //
   // The wall-clock anchor stays for the case with no ticks at all: a display
   // with no recorder, or a platform with no host, where the free-running loop
   // is what filled the ring.
   const endMs =
-    tickOriginMs === undefined
+    replayOriginWallMs === undefined
       ? current.clock.fromWallClockMs(triggerAtWallMs)
-      : tickOriginMs + Math.max(0, replayDurationMs)
+      : current.clock.fromWallClockMs(replayOriginWallMs) + Math.max(0, replayDurationMs)
   const startMs = endMs - Math.max(0, replayDurationMs)
   const freezeId = randomUUID()
   current.timeline.freeze(freezeId, startMs, endMs)
@@ -276,11 +278,21 @@ export function frozenObservations(
  */
 export function tickSurfaces(
   displayId: string,
-  frameMs: number,
+  frameWallMs: number,
   frameAgeMs?: number,
   tickDelayMs?: number,
 ): void {
-  runtime?.lane.tickAt(displayId, frameMs, frameAgeMs, tickDelayMs)
+  const current = runtime
+  if (current === null) return
+  // Every recorder is a separate renderer with its own performance origin.
+  // Normalize at the process boundary so Lane S, the provider host, freezes and
+  // queries all remain on the one SessionClock promised by this module.
+  current.lane.tickAt(
+    displayId,
+    current.clock.fromWallClockMs(frameWallMs),
+    frameAgeMs,
+    tickDelayMs,
+  )
 }
 
 export interface ContextStatus {
diff --git a/core/src/main/context/surfaceLane.ts b/core/src/main/context/surfaceLane.ts
index c9424a6..86abe3f 100644
--- a/core/src/main/context/surfaceLane.ts
+++ b/core/src/main/context/surfaceLane.ts
@@ -753,14 +753,19 @@ export class SurfaceLane {
       // renderer never sent it. They are now both measured, and the log states
       // each separately so a future displacement can be attributed rather than
       // argued about.
-      // A STABLE LAG, BECAUSE ONE TICK'S ROUND TRIP IS NOISE (#110).
+      // PER-TICK LAG, BECAUSE IT NAMES THIS RECTANGLE'S READ INSTANT (#110).
       //
-      // This used `lag` — THIS tick's round trip. `frameMs` arrives on an even,
-      // strictly increasing grid (presentationTime, ~67 ms apart at 15 fps), and
-      // adding a per-sample number that swings by tens of milliseconds to an
-      // even grid REORDERS it. The guard below then had to catch the inversion,
-      // and `Math.max` does not reorder — it COLLAPSES: the late sample is filed
-      // at the previous sample's exact millisecond.
+      // A rolling median was tried here to smooth scheduling jitter. It made an
+      // orderly-looking grid by assigning each real geometry sample somebody
+      // else's typical read time. Under a real host stall that is a position
+      // error proportional to window speed, and the synchronization harness
+      // catches it. `lag` is not decorative round-trip noise: it is the measured
+      // instant at which THIS rectangle came from the desk.
+      //
+      // Per-tick times can arrive out of order when requests overlap. The guard
+      // below therefore drops an older answer honestly; it must never clamp it
+      // forward, because `Math.max` does not reorder — it COLLAPSES: the late
+      // sample is filed at the previous sample's exact millisecond.
       //
       // Measured in the packs that were reported as wrong, counting samples that
       // share a time with another sample of the same window:
@@ -774,10 +779,12 @@ export class SurfaceLane {
       // other is discarded and the box holds the older rectangle across a whole
       // frame — while a shaken window travels ~443 px. "움직일때 어긋나".
       //
-      // The round trip's TYPICAL size is a real quantity and belongs in the
-      // sample time; its per-tick jitter is not a measurement of anything, it is
-      // scheduling noise, and it was the only thing making the grid non-monotone.
-      // So the median of the recent lags goes in and the jitter stays out.
+      // The round trip's PER-TICK size is a real quantity and belongs in that
+      // sample's time: this rectangle was read at that measured instant. Using
+      // a rolling median instead looks smoother but files a real geometry sample
+      // under somebody else's time whenever host scheduling varies; the
+      // synchronization harness models those stalls and catches the resulting
+      // speed error.
       // The full chain, every leg measured: the frame exists at `frameMs`; the
       // callback ran `delayMs` later; the host read the desk `lag` after that;
       // and the picture itself is `ageMs` older than its label. The observation
diff --git a/core/src/main/exporter.ts b/core/src/main/exporter.ts
index f5b30fc..01f6c84 100644
--- a/core/src/main/exporter.ts
+++ b/core/src/main/exporter.ts
@@ -1158,11 +1158,19 @@ function uniquePackDir(outputDir: string, date: Date): string {
  */
 export function copyAfterSave(mode: ClipboardAfterSave, dirPath: string): void {
   if (mode === 'off') return
-  if (mode === 'folder') {
-    copyFolderToClipboard(dirPath)
-    return
+  try {
+    if (mode === 'folder') {
+      copyFolderToClipboard(dirPath)
+      return
+    }
+    clipboard.writeText(mode === 'path' ? dirPath : analyzePackPrompt(dirPath))
+  } catch (err) {
+    // The pack is already complete on disk. Electron can refuse clipboard
+    // ownership (locked desktop, exiting process); turning that convenience
+    // failure into a rejected save tells the caller the durable capture failed
+    // and encourages a duplicate retry. Clipboard is explicitly best-effort.
+    console.error('capturepack: clipboard copy failed:', err)
   }
-  clipboard.writeText(mode === 'path' ? dirPath : analyzePackPrompt(dirPath))
 }
 
 function copyFolderToClipboard(dirPath: string): void {
diff --git a/core/src/main/historyWindow.ts b/core/src/main/historyWindow.ts
index 4fcbb30..7529346 100644
--- a/core/src/main/historyWindow.ts
+++ b/core/src/main/historyWindow.ts
@@ -54,13 +54,14 @@ function packArchiveExt(p: string): string | null {
   return PACK_ARCHIVE_EXTS.find((ext) => lower.endsWith(ext)) ?? null
 }
 
-/** The sibling archive of a pack FOLDER, whichever extension it was made with. */
-function siblingArchive(dirPath: string): string | null {
+/** Every sibling archive of a pack folder, newest and legacy suffixes alike. */
+function siblingArchives(dirPath: string): string[] {
+  const found: string[] = []
   for (const ext of PACK_ARCHIVE_EXTS) {
     const candidate = `${dirPath}${ext}`
-    if (fs.existsSync(candidate)) return candidate
+    if (fs.existsSync(candidate)) found.push(candidate)
   }
-  return null
+  return found
 }
 
 /** A zip entry's display name: its own basename, minus whichever suffix it has. */
@@ -277,10 +278,12 @@ export function registerHistoryIpc(live: Settings): void {
     // Zip twin: best-effort — the folder is already gone; a failure here must
     // not report the whole delete as failed.
     if (entry.kind === 'dir') {
-      const zip = siblingArchive(entry.path)
-      if (zip !== null) {
+      // Both can exist legitimately: older releases wrote `.capturepack`, then
+      // Create ZIP writes `.zip`. Leaving either one behind makes the deleted
+      // card reappear from the store as an orphaned archive.
+      for (const archive of siblingArchives(entry.path)) {
         try {
-          await shell.trashItem(zip)
+          await shell.trashItem(archive)
         } catch (err) {
           console.error('capturepack: could not trash zip twin:', errorMessage(err))
         }
@@ -486,7 +489,7 @@ function summarize(entry: RawPackEntry): HistoryPackSummary {
 
 function zipTwinPresent(entry: RawPackEntry): boolean {
   if (entry.kind === 'zip') return true
-  return siblingArchive(entry.path) !== null
+  return siblingArchives(entry.path).length > 0
 }
 
 /** Card title fallback (GOAL "History"): report.md's first sentence. */
@@ -599,7 +602,10 @@ async function renamePack(entry: RawPackEntry, rawName: string): Promise<History
       : path.join(parent, newName)
   // Case-only renames are legal on Windows; anything else must not overwrite.
   const caseOnly = target.toLowerCase() === entry.path.toLowerCase()
-  if (!caseOnly && (fs.existsSync(target) || (entry.kind === 'dir' && siblingArchive(target) !== null))) {
+  if (
+    !caseOnly &&
+    (fs.existsSync(target) || (entry.kind === 'dir' && siblingArchives(target).length > 0))
+  ) {
     return { ok: false, error: liveT()('history.errNameExists') }
   }
   try {
@@ -610,8 +616,7 @@ async function renamePack(entry: RawPackEntry, rawName: string): Promise<History
   // The zip twin follows the folder (contract). Best-effort: the folder rename
   // already succeeded and must not be reported as failed.
   if (entry.kind === 'dir') {
-    const oldZip = siblingArchive(entry.path)
-    if (oldZip !== null) {
+    for (const oldZip of siblingArchives(entry.path)) {
       try {
         // Keeps the suffix it already had: renaming a pack must not also
         // silently convert an older `.capturepack` archive into a `.zip`.
diff --git a/core/src/main/mcp/store.ts b/core/src/main/mcp/store.ts
index fcbe4bf..8f1752c 100644
--- a/core/src/main/mcp/store.ts
+++ b/core/src/main/mcp/store.ts
@@ -4,6 +4,7 @@
 import AdmZip from 'adm-zip'
 import * as fs from 'node:fs'
 import * as path from 'node:path'
+import { FORMAT_NAME } from '../../shared/types'
 import type { AnnotationsFile, Manifest, TimelineFile } from '../../shared/types'
 
 const PACK_EXT = '.capturepack'
@@ -25,15 +26,46 @@ function packArchiveExt(name: string): string | null {
   return PACK_ARCHIVE_EXTS.find((ext) => lower.endsWith(ext)) ?? null
 }
 
-/**
- * True when a zip has a manifest.json at its root.
- *
- * Reads the central directory only — adm-zip parses the entry table without
- * inflating anything, so this costs one small read per candidate file.
- */
+const MAX_INDEX_MANIFEST_BYTES = 4 * 1_048_576
+
+function manifestNamesCapturePack(raw: string): boolean {
+  try {
+    const value: unknown = JSON.parse(raw.replace(/^\uFEFF/, ''))
+    return (
+      typeof value === 'object' &&
+      value !== null &&
+      'format' in value &&
+      (value as { format?: unknown }).format === FORMAT_NAME
+    )
+  } catch {
+    return false
+  }
+}
+
+function directoryHoldsAPack(dirPath: string): boolean {
+  try {
+    const manifestPath = path.join(dirPath, 'manifest.json')
+    const stat = fs.statSync(manifestPath)
+    if (!stat.isFile() || stat.size > MAX_INDEX_MANIFEST_BYTES) return false
+    return manifestNamesCapturePack(fs.readFileSync(manifestPath, 'utf8'))
+  } catch {
+    return false
+  }
+}
+
+/** True only when the root manifest parses and identifies CapturePack. */
 function zipHoldsAPack(zipPath: string): boolean {
   try {
-    return new AdmZip(zipPath).getEntries().some((e) => e.entryName === 'manifest.json')
+    const zip = new AdmZip(zipPath)
+    const manifest = zip.getEntry('manifest.json')
+    if (
+      manifest === null ||
+      manifest.isDirectory ||
+      manifest.header.size > MAX_INDEX_MANIFEST_BYTES
+    ) {
+      return false
+    }
+    return manifestNamesCapturePack(manifest.getData().toString('utf8'))
   } catch {
     return false
   }
@@ -194,7 +226,7 @@ export function createPackStore(options: { outputDir: string; watch: boolean }):
     const dirStems = new Set<string>()
     for (const d of dirents) {
       try {
-        if (d.isDirectory() && fs.existsSync(path.join(dir, d.name, 'manifest.json'))) {
+        if (d.isDirectory() && directoryHoldsAPack(path.join(dir, d.name))) {
           dirStems.add(d.name.toLowerCase())
         }
       } catch {
@@ -208,18 +240,14 @@ export function createPackStore(options: { outputDir: string; watch: boolean }):
         const ext = d.isFile() ? packArchiveExt(d.name) : null
         if (ext !== null) {
           const stem = d.name.slice(0, -ext.length)
-          // A `.zip` is only a pack if it CONTAINS one. `.capturepack` could be
-          // taken on its name alone because nothing else writes that extension;
-          // now that archives are ordinary zips (the name has to say what the
-          // file is), the output folder may hold zips from anywhere, and
-          // indexing a holiday photo album as a capture would be worse than
-          // missing one. The check reads the archive's central directory, not
-          // its contents.
-          if (!dirStems.has(stem.toLowerCase()) && (ext === PACK_EXT || zipHoldsAPack(full))) {
+          // A suffix is not identity. History can rename and DELETE every item
+          // this index exposes, so both modern `.zip` and legacy `.capturepack`
+          // must carry a root manifest that actually names CapturePack.
+          if (!dirStems.has(stem.toLowerCase()) && zipHoldsAPack(full)) {
             out.push({ id: rel.slice(0, -ext.length), path: full, kind: 'zip', mtimeMs: fs.statSync(full).mtimeMs })
           }
         } else if (d.isDirectory()) {
-          if (fs.existsSync(path.join(full, 'manifest.json'))) {
+          if (directoryHoldsAPack(full)) {
             out.push({ id: rel, path: full, kind: 'dir', mtimeMs: fs.statSync(full).mtimeMs })
           } else if (depth < MAX_SCAN_DEPTH) {
             scanDir(full, rel, depth + 1, out)
diff --git a/core/src/main/session.ts b/core/src/main/session.ts
index 95aec93..5b21a36 100644
--- a/core/src/main/session.ts
+++ b/core/src/main/session.ts
@@ -182,7 +182,7 @@ interface FrozenDisplay {
   height: number
   replayWebm: Buffer | null
   replayDurationMs: number
-  /** The tick clock's value at this replay's t=0 (#112); absent if unknown. */
+  /** DOMHighRes wall-comparable time of this replay's t=0 (#112); absent if unknown. */
   replayOriginMs?: number
   replayMimeType: string | null
   replayFile: 'replay.webm' | 'replay.mp4' | null
@@ -493,14 +493,14 @@ async function runFlow(settings: Settings): Promise<void> {
   // configured length, and a range that claimed otherwise would put every pack
   // time a few seconds off. The delay costs nothing — retention keeps the
   // replay length plus a slack, and the prune runs at 1 Hz.
-  // The tick clock's value at the SAVED replay's t=0, plus whatever the exact
-  // -length cut drops from its head — that is where the pack clock starts (#112).
+  // The cross-renderer tick axis at the SAVED replay's t=0, plus whatever the
+  // exact-length cut drops from its head — that is where pack time starts (#112).
   const focusedReplay = frozen.displays.find((d) => d.focused)
-  const tickOriginMs =
+  const replayOriginWallMs =
     focusedReplay?.replayOriginMs === undefined
       ? undefined
       : focusedReplay.replayOriginMs + replaySourceStartMs
-  const contextFreezeId = freezeContext(replayEndAt, replayDurationMs, tickOriginMs)
+  const contextFreezeId = freezeContext(replayEndAt, replayDurationMs, replayOriginWallMs)
   logInfo(
     `[context] pack clock: replay ends ${String(replayEndAt - triggerAt)} ms after the trigger, ` +
       `${String(replayDurationMs)} ms long (raw ${String(rawReplayDurationMs)} ms)`,
@@ -663,7 +663,7 @@ async function runFlow(settings: Settings): Promise<void> {
   // when there is something to declare — a capture made with no browser
   // talking has no chrome-dom directory at all, which SPEC §11.3 reads as
   // "nobody was watching" rather than "nothing happened".
-  void (async () => {
+  const domWrite: Promise<void> = (async () => {
     const saved = handle
     if (saved === null) return
     const window = frozenWindow(contextFreezeId)
@@ -684,6 +684,11 @@ async function runFlow(settings: Settings): Promise<void> {
     })
     if (!wrote) return
     try {
+      // addManifestPlugin is a whole-file read/modify/write. UIA was started
+      // first and may still be declaring itself; serialize the two appenders so
+      // each reads the other's result instead of both publishing a manifest
+      // derived from the same pre-plugin snapshot.
+      await uiaWrite
       await addManifestPlugin(saved, domPluginDeclaration())
       logInfo(`[chrome] pack carries ${events.length} DOM event(s) from the browser`)
     } catch (err) {
@@ -912,7 +917,7 @@ async function runFlow(settings: Settings): Promise<void> {
     // renderer even though there are no annotations to finalize.
     const savedHandle = handle
     if (savedHandle !== null && replay !== null && replaySourceStartMs > 0) {
-      void uiaWrite
+      void Promise.all([uiaWrite, domWrite])
         .then(() =>
           finalizeCancelledExactReplay(
             savedHandle,
@@ -930,10 +935,12 @@ async function runFlow(settings: Settings): Promise<void> {
     return
   }
 
-  // Both writers of manifest.json must never interleave: the save-first plugin
-  // declaration above patches it in place, updatePack below rewrites it whole.
-  // Awaiting here costs nothing — the dump resolved long before the user saved.
-  const uiaPayload = await uiaWrite
+  // Every writer of manifest.json must finish before updatePack rewrites it
+  // whole. UIA and Chrome DOM both append a plugin declaration to the
+  // save-first manifest; awaiting only the slower-looking UIA promise left a
+  // fast editor save able to race Chrome's read/modify/write and silently lose
+  // either that declaration or the user's finalized manifest.
+  const [uiaPayload] = await Promise.all([uiaWrite, domWrite])
 
   // The replay is ALWAYS kept when one exists (GOAL "No include-replay
   // toggle"): what leaves the machine is decided at share time, not here. It
diff --git a/core/src/main/settingsWindow.ts b/core/src/main/settingsWindow.ts
index 9ed6159..c7d1903 100644
--- a/core/src/main/settingsWindow.ts
+++ b/core/src/main/settingsWindow.ts
@@ -4,10 +4,12 @@
 // (applyPartial, same per-key rules as settings.json loading), written to disk,
 // applied to the live settings object index.ts shares with every flow, and the
 // recorder-window set is rebuilt when a change affects it.
+import AdmZip from 'adm-zip'
 import { app, BrowserWindow, clipboard, dialog, ipcMain, screen, shell } from 'electron'
+import { createHash } from 'node:crypto'
 import * as fs from 'node:fs'
 import * as path from 'node:path'
-import { DOM_PROTOCOL_VERSION, domBridgeStatus } from './chrome/domBridge'
+import { DOM_PROTOCOL_VERSION, domBridgeStatus, setDomRetention } from './chrome/domBridge'
 import {
   extensionDir,
   bundledExtensionVersion,
@@ -26,10 +28,12 @@ import type {
   SettingsGetResult,
   SettingsSetResult,
   SettingsStatusResult,
+  StoragePurgeRequest,
   StoragePurgeResult,
   StorageUsage,
 } from '../shared/ipc'
 import { resolveLanguage } from '../shared/i18n'
+import { FORMAT_NAME } from '../shared/types'
 import type { Settings } from '../shared/types'
 import { logError, logInfo, logWarn } from './log'
 import { restartCapture } from './capture'
@@ -61,14 +65,53 @@ interface StoredPack {
   mtimeMs: number
 }
 
+/** A manifest large enough to exceed this is not one Settings should inflate. */
+const MAX_PURGE_MANIFEST_BYTES = 4 * 1_048_576
+
+/** The one identifying field purge needs; malformed JSON is simply not a pack. */
+function manifestNamesCapturePack(raw: string): boolean {
+  try {
+    const value: unknown = JSON.parse(raw.replace(/^\uFEFF/, ''))
+    return (
+      typeof value === 'object' &&
+      value !== null &&
+      'format' in value &&
+      (value as { format?: unknown }).format === FORMAT_NAME
+    )
+  } catch {
+    return false
+  }
+}
+
+function directoryHoldsCapturePack(dir: string): boolean {
+  const manifestPath = path.join(dir, 'manifest.json')
+  const stat = fs.statSync(manifestPath)
+  if (!stat.isFile() || stat.size > MAX_PURGE_MANIFEST_BYTES) return false
+  return manifestNamesCapturePack(fs.readFileSync(manifestPath, 'utf8'))
+}
+
+function archiveHoldsCapturePack(archivePath: string): boolean {
+  const zip = new AdmZip(archivePath)
+  const manifest = zip.getEntry('manifest.json')
+  if (
+    manifest === null ||
+    manifest.isDirectory ||
+    manifest.header.size > MAX_PURGE_MANIFEST_BYTES
+  ) {
+    return false
+  }
+  return manifestNamesCapturePack(manifest.getData().toString('utf8'))
+}
+
 /**
  * Every CapturePack in the output folder — and NOTHING else in it.
  *
- * A folder counts only when it holds a manifest.json, and an archive only when
- * its name matches one of ours. The output folder is a place the user also
- * keeps their own things (this machine's is the Desktop), and a storage tool
- * that measured or deleted by "everything in this directory" would be a
- * catastrophe waiting for its first Downloads folder.
+ * A filename or a file named manifest.json is not identity: projects commonly
+ * have manifests, and any downloaded archive can end in .zip. The manifest at
+ * the pack root has to parse and name CapturePack before this destructive path
+ * may even count it. The output folder is a place the user also keeps their own
+ * things (this machine's may be the Desktop), and a storage tool that guessed
+ * by shape would eventually put somebody's unrelated work in the Recycle Bin.
  */
 function listStoredPacks(outputDir: string): StoredPack[] {
   let entries: fs.Dirent[]
@@ -82,9 +125,10 @@ function listStoredPacks(outputDir: string): StoredPack[] {
     const full = path.join(outputDir, entry.name)
     try {
       if (entry.isDirectory()) {
-        if (!fs.existsSync(path.join(full, 'manifest.json'))) continue
+        if (!directoryHoldsCapturePack(full)) continue
         packs.push({ path: full, bytes: dirBytes(full), mtimeMs: fs.statSync(full).mtimeMs })
       } else if (entry.isFile() && /\.(zip|capturepack)$/i.test(entry.name)) {
+        if (!archiveHoldsCapturePack(full)) continue
         const stat = fs.statSync(full)
         packs.push({ path: full, bytes: stat.size, mtimeMs: stat.mtimeMs })
       }
@@ -116,6 +160,32 @@ function dirBytes(dir: string): number {
   return total
 }
 
+interface PurgeBucket {
+  packs: StoredPack[]
+  bytes: number
+  revision: string
+}
+
+/**
+ * A confirmable target set, not merely a count.
+ *
+ * Paths stay private to main; the renderer gets only their digest. Size and
+ * mtime make a pack changed in place a different set too. Sorting makes the
+ * token independent of directory enumeration order.
+ */
+function purgeBucket(packs: readonly StoredPack[], days: number, now: number): PurgeBucket {
+  const cutoff = now - days * 86_400_000
+  const old = packs.filter((p) => p.mtimeMs < cutoff)
+  const identity = old
+    .map((p) => [path.resolve(p.path), p.bytes, p.mtimeMs] as const)
+    .sort(([a], [b]) => a.localeCompare(b))
+  return {
+    packs: old,
+    bytes: old.reduce((sum, p) => sum + p.bytes, 0),
+    revision: createHash('sha256').update(JSON.stringify(identity)).digest('hex'),
+  }
+}
+
 function storageUsage(outputDir: string): StorageUsage {
   const packs = listStoredPacks(outputDir)
   const now = Date.now()
@@ -123,9 +193,13 @@ function storageUsage(outputDir: string): StorageUsage {
     totalBytes: packs.reduce((sum, p) => sum + p.bytes, 0),
     totalPacks: packs.length,
     olderThan: PURGE_AGES_DAYS.map((days) => {
-      const cutoff = now - days * 86_400_000
-      const old = packs.filter((p) => p.mtimeMs < cutoff)
-      return { days, packs: old.length, bytes: old.reduce((sum, p) => sum + p.bytes, 0) }
+      const bucket = purgeBucket(packs, days, now)
+      return {
+        days,
+        packs: bucket.packs.length,
+        bytes: bucket.bytes,
+        revision: bucket.revision,
+      }
     }),
   }
 }
@@ -138,9 +212,25 @@ function storageUsage(outputDir: string): StorageUsage {
  * this a decision the user can take back — and if the shell refuses, the pack
  * stays where it is rather than being removed some other way.
  */
-async function purgeOlderThan(outputDir: string, days: number): Promise<StoragePurgeResult> {
-  const cutoff = Date.now() - days * 86_400_000
-  const doomed = listStoredPacks(outputDir).filter((p) => p.mtimeMs < cutoff)
+async function purgeOlderThan(
+  outputDir: string,
+  days: number,
+  expectedRevision: string,
+): Promise<StoragePurgeResult> {
+  // RESCAN AFTER CONFIRMATION. A capture can finish while the native confirm
+  // dialog is open, and an old pack can cross the cutoff. Neither belongs to
+  // the set the user saw. Refusing on a changed digest turns that race into one
+  // harmless extra click after the panel refreshes.
+  const bucket = purgeBucket(listStoredPacks(outputDir), days, Date.now())
+  if (bucket.revision !== expectedRevision) {
+    return {
+      ok: false,
+      packsDeleted: 0,
+      bytesFreed: 0,
+      error: 'storage changed; review the updated count',
+    }
+  }
+  const doomed = bucket.packs
   let packsDeleted = 0
   let bytesFreed = 0
   let firstError: string | null = null
@@ -372,6 +462,10 @@ export function registerSettingsIpc(live: Settings, hooks: SettingsIpcHooks = {}
     // the ring immediately rather than at the next launch).
     if (live.replaySeconds !== before.replaySeconds) {
       updateContextRetention(live.replaySeconds * 1000)
+      // The browser event ring shares the replay boundary. Updating only Lane S
+      // left Chrome DOM at its startup retention, so increasing replay length
+      // produced a longer video whose first half could never carry a pick.
+      setDomRetention(live.replaySeconds * 1000)
     }
     // The updater honors the toggle live (GOAL: instant apply where possible).
     if (live.autoUpdateCheck !== before.autoUpdateCheck) {
@@ -486,23 +580,31 @@ export function registerSettingsIpc(live: Settings, hooks: SettingsIpcHooks = {}
 
   // Storage (GOAL "Settings GUI"): the app that fills the folder is the one
   // that should be able to empty it. Both handlers walk the SAME set of
-  // entries — a pack is a folder holding a manifest, or an archive beside one —
-  // so nothing else the user keeps in that folder is ever counted or touched.
+  // entries — a directory or archive whose ROOT manifest parses and identifies
+  // CapturePack — so nothing else the user keeps there is counted or touched.
   ipcMain.handle(IPC.settingsStorageUsage, async (): Promise<StorageUsage> => {
     return storageUsage(live.outputDir)
   })
 
   ipcMain.handle(
     IPC.settingsStoragePurge,
-    async (_event, days: unknown): Promise<StoragePurgeResult> => {
-      const olderThanDays = typeof days === 'number' && Number.isFinite(days) ? days : NaN
+    async (_event, value: unknown): Promise<StoragePurgeResult> => {
+      const request =
+        typeof value === 'object' && value !== null ? (value as Partial<StoragePurgeRequest>) : {}
+      const olderThanDays =
+        typeof request.days === 'number' && Number.isFinite(request.days) ? request.days : NaN
       // Zero is now a button of its own — "Delete everything" — and it says so
-      // before it runs. Negative is still nothing this panel can mean, and a
-      // value this function does not recognise deletes nothing.
-      if (!(olderThanDays >= 0)) {
+      // before it runs. This IPC is still a trust boundary: ONLY the four ages
+      // the panel displayed are meaningful, and only the exact set it confirmed
+      // may reach the shell.
+      if (
+        !PURGE_AGES_DAYS.includes(olderThanDays) ||
+        typeof request.revision !== 'string' ||
+        !/^[0-9a-f]{64}$/.test(request.revision)
+      ) {
         return { ok: false, packsDeleted: 0, bytesFreed: 0, error: 'unsupported age' }
       }
-      return purgeOlderThan(live.outputDir, olderThanDays)
+      return purgeOlderThan(live.outputDir, olderThanDays, request.revision)
     },
   )
 
diff --git a/core/src/main/updater.ts b/core/src/main/updater.ts
index c5e9e9a..742076d 100644
--- a/core/src/main/updater.ts
+++ b/core/src/main/updater.ts
@@ -133,7 +133,10 @@ export function initUpdater(opts: {
     // fetched.
     if (/no published versions/i.test(message)) {
       logInfo('[updater] no releases published yet — nothing to update to')
-      setStatus({ state: 'idle' })
+      // Manual checks need the same readable terminal state as an ordinary
+      // update-not-available event. `idle` immediately erases the answer from
+      // the tray, contradicting the branch's own "up to date" meaning.
+      setStatus({ state: 'up-to-date' })
       return
     }
     // Log and report only — a failed check or download leaves the running
diff --git a/core/src/preload/settings.ts b/core/src/preload/settings.ts
index 9cf90c2..65fcd4b 100644
--- a/core/src/preload/settings.ts
+++ b/core/src/preload/settings.ts
@@ -2,6 +2,7 @@
 import { contextBridge, ipcRenderer } from 'electron'
 import { IPC } from '../shared/ipc'
 import type {
+  StoragePurgeRequest,
   StoragePurgeResult,
   StorageUsage,
   ChromeIntegrationStatus,
@@ -49,8 +50,8 @@ contextBridge.exposeInMainWorld('settingsBridge', {
   storageUsage(): Promise<StorageUsage> {
     return ipcRenderer.invoke(IPC.settingsStorageUsage) as Promise<StorageUsage>
   },
-  storagePurge(days: number): Promise<StoragePurgeResult> {
-    return ipcRenderer.invoke(IPC.settingsStoragePurge, days) as Promise<StoragePurgeResult>
+  storagePurge(request: StoragePurgeRequest): Promise<StoragePurgeResult> {
+    return ipcRenderer.invoke(IPC.settingsStoragePurge, request) as Promise<StoragePurgeResult>
   },
   chromeCopyPath(): void {
     ipcRenderer.send(IPC.settingsChromeCopyPath)
diff --git a/core/src/renderer/capture/capture.ts b/core/src/renderer/capture/capture.ts
index 481f6f8..d309396 100644
--- a/core/src/renderer/capture/capture.ts
+++ b/core/src/renderer/capture/capture.ts
@@ -381,7 +381,7 @@ function startFrameTicks(): void {
       video.requestVideoFrameCallback(pump)
       return
     }
-    // ONE MONOTONIC NUMBER FOR THE WHOLE SESSION (#112).
+    // ONE COMPARABLE MONOTONIC NUMBER FOR EVERY RECORDER (#112).
     //
     // This used to send the frame's position within the CURRENT recorder slot.
     // The recorder rotates slots every `segmentSeconds`, and at each rotation
@@ -389,11 +389,13 @@ function startFrameTicks(): void {
     // BACKWARDS by up to thirty seconds. The log said it plainly once a
     // recording lived long enough to rotate: "736 samples over -9s".
     //
-    // `presentationTime` is `performance.now()`-based and never goes backwards,
-    // so it is sent as-is. Turning it into a position in the saved file needs
-    // to know WHICH slot was saved and where that slot began, and the only
-    // moment both are known is when the replay is handed over — so that is
-    // where the subtraction happens, not here.
+    // `presentationTime` is monotonic but LOCAL to this renderer. Recorder
+    // windows are separate JavaScript globals, so sending it bare makes a ring
+    // ticked by display A incomparable with a replay frozen from display B — a
+    // normal case when the cursor moves between recorder creation and capture.
+    // Adding the fixed `timeOrigin` keeps the monotonic delta while putting all
+    // recorder windows on the DOMHighRes wall-comparable axis. Main translates
+    // that axis once into its SessionClock before it reaches the ring.
     // HOW OLD THE PICTURE ALREADY IS (#108), measured at last.
     //
     // The log has printed "frame already 0 ms old" for every capture of this
@@ -436,7 +438,7 @@ function startFrameTicks(): void {
     const delayMs = Math.max(0, now - submitted)
     window.captureBridge.sendTick?.({
       displayId: startPayload?.displayId ?? '',
-      mediaTimeMs: submitted,
+      mediaTimeMs: performance.timeOrigin + submitted,
       tickDelayMs: delayMs,
       ...(ageMs === undefined ? {} : { frameAgeMs: ageMs }),
     })
@@ -745,9 +747,10 @@ async function handleReplayRequest(requestId: string): Promise<void> {
     recorder.stop()
   })
   const durationMs = Math.round(performance.now() - startedAt)
-  // Where this file's t=0 sits on the tick clock (#112). Ticks are monotonic
-  // `presentationTime`; these bytes begin when this slot did.
-  const originMs = startedAt
+  // Where this file's t=0 sits on the cross-renderer tick axis (#112). A bare
+  // `startedAt` belongs only to this renderer; the fixed origin makes a replay
+  // from another display comparable before main translates it to SessionClock.
+  const originMs = performance.timeOrigin + startedAt
   startSlot(slot) // restart before assembling so buffering never pauses
   restaggerSurvivor(slots[slot.index === 0 ? 1 : 0])
   const buffer = await new Blob(chunks, { type: format.mimeType }).arrayBuffer()
diff --git a/core/src/renderer/editor/editor.html b/core/src/renderer/editor/editor.html
index 0a9f82c..b1da14a 100644
--- a/core/src/renderer/editor/editor.html
+++ b/core/src/renderer/editor/editor.html
@@ -268,6 +268,7 @@
            the moment of the drag. Floated over the track it costs no width at
            all, and it can never push the slider around. -->
       <span id="trimChip" hidden></span>
+    </div>
     <span id="timeLabel" data-i18n="editor.now">now</span>
     <span id="scrubHint" data-i18n="editor.loadingReplay">loading replay…</span>
   </footer>
diff --git a/core/src/renderer/editor/editor.ts b/core/src/renderer/editor/editor.ts
index a16bfe0..93a44b6 100644
--- a/core/src/renderer/editor/editor.ts
+++ b/core/src/renderer/editor/editor.ts
@@ -2326,7 +2326,12 @@ interface AimAt {
  * pixel of it holds a smaller control.
  */
 function pickBeatsBox(picked: PickableObject, a: Annotation, at: AimAt): boolean {
-  if (a.annotation_id === state.selectedId) return false
+  // A semantic pick is selected as soon as its pending text is committed. If
+  // selection won unconditionally, that first window box swallowed the very
+  // next click in its middle and no inner control could ever refine it. Core
+  // still owns tracked geometry, so its empty middle remains a pick surface;
+  // hand-drawn/untracked selections keep the whole-box move gesture.
+  if (a.annotation_id === state.selectedId && !trackedSurfaces.has(a.annotation_id)) return false
   if (at.repeat === true) return false
   if (onBoxEdge(a, at.x, at.y, at.ui)) return false
   if (annotatesPick(a, picked)) return false
diff --git a/core/src/renderer/editor/lifetime.ts b/core/src/renderer/editor/lifetime.ts
index aced964..94f75c9 100644
--- a/core/src/renderer/editor/lifetime.ts
+++ b/core/src/renderer/editor/lifetime.ts
@@ -46,16 +46,18 @@ export function lifetimeMidpoint(a: Annotation, replayDurationMs: number): numbe
  * is where the track is anchored (SPEC §8.3), and a lifetime that begins there
  * means the first frame the box is drawn on is the frame it was picked from.
  *
- * Clamped at the END of the replay by moving the START back, so a box created
- * near "now" keeps its full duration instead of being silently shortened.
+ * Clamped at the END of the replay by shortening the interval. Moving the
+ * start backwards would invent visibility before the click — exactly the
+ * centering failure this helper exists to prevent. There is no future footage
+ * after "now", so a tail annotation honestly has a shorter recorded lifetime.
  */
 export function lifetimeFrom(
   anchorMs: number,
   durationMs: number,
   replayDurationMs: number,
 ): { start_ms: number; end_ms: number } {
-  const duration = Math.min(durationMs, replayDurationMs)
-  const start = Math.max(0, Math.min(Math.round(anchorMs), replayDurationMs - duration))
+  const duration = Math.max(0, Math.min(durationMs, replayDurationMs))
+  const start = Math.max(0, Math.min(Math.round(anchorMs), replayDurationMs))
   return { start_ms: start, end_ms: Math.min(replayDurationMs, Math.round(start + duration)) }
 }
 
diff --git a/core/src/renderer/editor/scrub.ts b/core/src/renderer/editor/scrub.ts
index 75cfc6a..657b614 100644
--- a/core/src/renderer/editor/scrub.ts
+++ b/core/src/renderer/editor/scrub.ts
@@ -131,6 +131,10 @@ export class ScrubController {
   /** Whether the frame callback drives the base draw (see trackPresentedFrames). */
   private frameDriven = false
   private frameGuard = 0
+  // `seeked` says the media position moved; it does NOT say the compositor has
+  // presented that frame. Export settlement includes this second half so Enter
+  // immediately after a scrub cannot save the previous pixels under a new time.
+  private framePending = false
 
   constructor(
     webm: ArrayBuffer,
@@ -175,6 +179,8 @@ export class ScrubController {
     })
     video.addEventListener('error', () => {
       this.failed = true
+      this.framePending = false
+      window.clearTimeout(this.frameGuard)
       this.notifySettled() // in-flight seeks will never complete; release waiters
       this.host.onState()
     })
@@ -183,6 +189,11 @@ export class ScrubController {
       // The picture and its time, together, before anything can read either.
       if (!this.showingNative) this.host.drawFrame(video)
       this.host.onFrame?.()
+      if (this.framePending) {
+        this.framePending = false
+        window.clearTimeout(this.frameGuard)
+        this.notifySettled()
+      }
     })
     this.video = video
   }
@@ -286,9 +297,10 @@ export class ScrubController {
   }
 
   /**
-   * Resolves once no seek is pending or in flight — i.e. the painted frame
-   * matches tMs. Export waits on this so a wheel burst right before Enter
-   * cannot ship a stale frame under a newer snapshot_t_ms.
+   * Resolves once no seek is pending or in flight AND, on a frame-driven
+   * renderer, the compositor has presented and painted its result. Export waits
+   * on both halves so a wheel burst right before Enter cannot ship a stale frame
+   * under a newer snapshot_t_ms.
    */
   whenSettled(): Promise<void> {
     if (this.settled) return Promise.resolve()
@@ -296,7 +308,10 @@ export class ScrubController {
   }
 
   private get settled(): boolean {
-    return this.failed || (this.pendingSeekMs === null && !this.seekInFlight)
+    return (
+      this.failed ||
+      (this.pendingSeekMs === null && !this.seekInFlight && !this.framePending)
+    )
   }
 
   private notifySettled(): void {
@@ -412,6 +427,8 @@ export class ScrubController {
     this.showingNative = true
     this.tMs = this.durationMs
     this.pendingSeekMs = null
+    this.framePending = false
+    window.clearTimeout(this.frameGuard)
     this.notifySettled()
     this.host.drawFrame('native')
     this.host.onState()
@@ -424,6 +441,9 @@ export class ScrubController {
     this.frameGuard = window.setTimeout(() => {
       if (this.presentedRawMs !== before || this.showingNative) return
       this.host.drawFrame(this.video)
+      this.host.onFrame?.()
+      this.framePending = false
+      this.notifySettled()
     }, 250)
   }
 
@@ -488,8 +508,12 @@ export class ScrubController {
       // callback is given a moment and then overruled. It has never been
       // observed to miss a seeked frame; this exists so that if it ever does,
       // the editor is stale rather than blank.
-      if (this.frameDriven) this.guardFrameDraw()
-      else this.host.drawFrame(this.video)
+      if (this.frameDriven) {
+        this.framePending = true
+        this.guardFrameDraw()
+      } else {
+        this.host.drawFrame(this.video)
+      }
       this.pumpSeek() // may put another seek in flight; settled stays false then
     }
     this.notifySettled()
@@ -531,6 +555,7 @@ class SlaveReplay {
   /** Whether the frame callback drives this screen's draw (see the master). */
   private frameDriven = false
   private frameGuard = 0
+  private framePending = false
   private readonly draw: (source: HTMLVideoElement | 'native') => void
   private showingNative = true
   private pendingSeekMs: number | null = null
@@ -562,6 +587,11 @@ class SlaveReplay {
     this.frameDriven = trackPresentedFrames(video, (mediaTimeMs) => {
       this.presentedRawMs = mediaTimeMs
       if (!this.showingNative) this.draw(video)
+      if (this.framePending) {
+        this.framePending = false
+        window.clearTimeout(this.frameGuard)
+        this.notifySettled()
+      }
     })
     video.addEventListener(
       'loadedmetadata',
@@ -582,6 +612,8 @@ class SlaveReplay {
     video.addEventListener('seeked', () => this.onSeeked())
     video.addEventListener('error', () => {
       this.failed = true
+      this.framePending = false
+      window.clearTimeout(this.frameGuard)
       this.notifySettled()
     })
     this.video = video
@@ -594,6 +626,8 @@ class SlaveReplay {
     this.showingNative = true
     this.lastTargetMs = null
     this.pendingSeekMs = null
+    this.framePending = false
+    window.clearTimeout(this.frameGuard)
     this.notifySettled()
     this.draw('native')
   }
@@ -651,7 +685,10 @@ class SlaveReplay {
   }
 
   private get settled(): boolean {
-    return this.failed || (this.pendingSeekMs === null && !this.seekInFlight)
+    return (
+      this.failed ||
+      (this.pendingSeekMs === null && !this.seekInFlight && !this.framePending)
+    )
   }
 
   private notifySettled(): void {
@@ -690,11 +727,14 @@ class SlaveReplay {
     this.seekInFlight = false
     if (!this.showingNative && this.video.paused) {
       if (this.frameDriven) {
+        this.framePending = true
         const before = this.presentedRawMs
         window.clearTimeout(this.frameGuard)
         this.frameGuard = window.setTimeout(() => {
           if (this.presentedRawMs !== before || this.showingNative) return
           this.draw(this.video)
+          this.framePending = false
+          this.notifySettled()
         }, 250)
       } else {
         this.draw(this.video)
diff --git a/core/src/renderer/settings/settings.ts b/core/src/renderer/settings/settings.ts
index d3af64e..f762f4a 100644
--- a/core/src/renderer/settings/settings.ts
+++ b/core/src/renderer/settings/settings.ts
@@ -10,6 +10,7 @@
 // because both move while the window is open.
 import type {
   ChromeIntegrationStatus,
+  StoragePurgeRequest,
   StoragePurgeResult,
   StorageUsage,
   McpStatus,
@@ -44,7 +45,7 @@ interface SettingsBridge {
   chromeUninstall(): Promise<ChromeIntegrationStatus>
   chromeOpenFolder(): void
   storageUsage(): Promise<StorageUsage>
-  storagePurge(days: number): Promise<StoragePurgeResult>
+  storagePurge(request: StoragePurgeRequest): Promise<StoragePurgeResult>
   chromeOpenExtensionsPage(): Promise<string | null>
   chromeCopyPath(): void
   chromeDetect(): Promise<ChromeIntegrationStatus>
@@ -912,7 +913,9 @@ for (const btn of storageRow.querySelectorAll<HTMLButtonElement>('[data-purge]')
       )
       if (!ok) return
       btn.disabled = true
-      const result = await bridge.storagePurge(days).catch(() => null)
+      const result = await bridge
+        .storagePurge({ days, revision: bucket.revision })
+        .catch(() => null)
       storageResult.hidden = false
       storageResult.textContent =
         result === null || !result.ok
diff --git a/core/src/shared/ipc.ts b/core/src/shared/ipc.ts
index c7f64c6..9cda9b0 100644
--- a/core/src/shared/ipc.ts
+++ b/core/src/shared/ipc.ts
@@ -254,11 +254,12 @@ export interface CaptureStartPayload {
   // requesting webContents); the renderer carries this for diagnostics only.
   displayId: string
   /**
-   * Whether this is the display that owns the pack clock (SPEC §10.1).
+   * Whether this recorder is the one source that ticks the surface ring.
    *
-   * Only that one ticks the surface ring (#105): a second display's frames
-   * carry a different recording's numbers, and filing samples under those would
-   * put the ring on two clocks at once — which is the problem, not the fix.
+   * This is an acquisition/load choice, not the display a future capture must
+   * focus. Main normalizes every renderer's DOMHighRes timestamps to its one
+   * SessionClock, and Lane S accepts only this source so a multi-monitor desk
+   * still pays for one frame callback chain (#105).
    */
   focused?: boolean
   fps: number
@@ -367,12 +368,13 @@ export interface CaptureFramesPayload {
 export interface CaptureTickPayload {
   displayId: string
   /**
-   * The frame's position in the file being recorded, in ms.
+   * The frame's DOMHighRes wall-comparable timestamp, in ms.
    *
-   * `presentationTime - slot.startedAt`, both on `performance.now()` — NOT the
-   * track's `mediaTime`, which starts when the stream did rather than when this
-   * recorder slot did and which the spec allows to be zero for a live source
-   * (#109).
+   * `performance.timeOrigin + presentationTime`: still monotonic inside the
+   * renderer, but comparable across the separate recorder windows. Main maps it
+   * to SessionClock before the surface lane sees it. NOT the track's
+   * `mediaTime`, which starts when the stream did and may be zero for a live
+   * source (#109).
    */
   mediaTimeMs: number
   /**
@@ -402,13 +404,14 @@ export interface CaptureTickPayload {
 export interface CaptureReplayResultPayload {
   requestId: string
   /**
-   * The tick clock's value at this replay's t=0 (#112).
+   * The DOMHighRes wall-comparable timestamp of this replay's t=0 (#112).
    *
-   * Ticks carry a session-monotonic `presentationTime`, because a per-slot
-   * number falls back to zero at every rotation and sent the ring's time
-   * BACKWARDS. Turning that into a position in THIS file needs to know which
+   * Ticks carry `timeOrigin + presentationTime`, because a per-slot number falls
+   * back to zero at every rotation and bare `performance.now()` belongs to one
+   * renderer only. Turning that into a position in THIS file needs to know which
    * slot was saved and where it began, and this is the only moment both are
-   * known. Subtract it from a tick to get a position in these bytes.
+   * known. Main maps it to SessionClock; subtract it from a mapped tick to get a
+   * position in these bytes.
    *
    * Absent where the recorder could not say, in which case the ring keeps its
    * own clock and nothing is mis-stated.
@@ -946,7 +949,7 @@ export interface ChromeIntegrationStatus {
   listening: boolean
   /** A host process has dialled in at least once this run. */
   hostSeen: boolean
-  /** The extension completed its handshake. */
+  /** A currently live native-host socket completed the extension handshake. */
   extensionConnected: boolean
   extensionVersion: string | null
   /**
@@ -1080,14 +1083,26 @@ export interface UiaPluginStatus {
  *
  * The per-age counts are computed alongside the total in ONE walk of the
  * folder, so the three buttons can each state their own consequence without
- * three separate scans — and so the number the user is shown is from the same
- * moment as the number the delete will act on.
+ * three separate scans. Each count carries a target-set revision; delete
+ * rescans and refuses if the consequence changed while confirmation was open.
  */
 export interface StorageUsage {
   totalBytes: number
   totalPacks: number
-  /** Keyed by age in days: what deleting everything older than that removes. */
-  olderThan: { days: number; packs: number; bytes: number }[]
+  /**
+   * Keyed by age in days: what deleting everything older than that removes.
+   *
+   * `revision` identifies the exact target set behind the displayed count.
+   * Purge has to present it back: captures are produced while Settings is open,
+   * and deleting a set the user never confirmed is not an acceptable race.
+   */
+  olderThan: { days: number; packs: number; bytes: number; revision: string }[]
+}
+
+export interface StoragePurgeRequest {
+  days: number
+  /** Opaque target-set revision returned by `settings:storage-usage`. */
+  revision: string
 }
 
 export interface StoragePurgeResult {
diff --git a/extensions/chrome/background.js b/extensions/chrome/background.js
index bb60aed..3f5f6fd 100644
--- a/extensions/chrome/background.js
+++ b/extensions/chrome/background.js
@@ -6,8 +6,10 @@ const HOST = 'com.capturepack.host'
 const PROTOCOL = 1
 
 let port = null
-// When the hello was last written on the CURRENT port, or null when this
-// extension is not holding a connection it has proved. See the alarm below.
+// When the app's hello was last RECEIVED on the current port, or null when this
+// extension is not holding a connection it has proved. An outbound write only
+// proves Chrome accepted bytes into a Port object; it says nothing about the
+// host, the named pipe, or the app at the other end.
 let handshakeAt = null
 // How long to wait before dialling again after a failed or dropped connection.
 // It backs off so a browser running without CapturePack installed does not
@@ -73,7 +75,7 @@ chrome.alarms.onAlarm.addListener((alarm) => {
   // mid-handshake — which is exactly what an installer replacing the executable
   // does — the extension can be left holding a port it believes in and never
   // dials again. `handshakeAt` is the only proof of a LIVE connection: it is
-  // set when the hello is written and cleared on every disconnect, so a port
+  // set only when the app's hello comes back and cleared on every disconnect, so a port
   // that has not proved itself by the time this alarm comes round is dropped
   // and redialled rather than trusted.
   if (port && handshakeAt === null) {
@@ -90,24 +92,45 @@ chrome.alarms.onAlarm.addListener((alarm) => {
 function connect() {
   if (port) return port
   try {
-    port = chrome.runtime.connectNative(HOST)
-    port.onDisconnect.addListener(() => {
+    const connected = chrome.runtime.connectNative(HOST)
+    port = connected
+    handshakeAt = null
+    connected.onMessage.addListener((message) => {
+      if (port !== connected) return
+      if (
+        !message ||
+        message.type !== 'host.hello' ||
+        message.protocol !== PROTOCOL ||
+        message.app !== 'capturepack'
+      ) {
+        return
+      }
+      handshakeAt = Date.now()
+      retryMs = RETRY_MIN_MS
+      chrome.action.setBadgeText({ text: '' })
+    })
+    connected.onDisconnect.addListener(() => {
+      // `lastError` exists only during this callback. Keep its exact text: the
+      // two-second redial cadence says only that a disconnect happened; Chrome
+      // is the component that can say whether it was EOF, a bad native frame,
+      // or a host launch failure.
+      const detail = chrome.runtime.lastError && chrome.runtime.lastError.message
+      if (port !== connected) return
       // Host not installed, app not running, or this extension's ID not yet in
       // the host manifest. All three are temporary, and all three used to end
       // here — see the note on the startup connect below.
       port = null
       handshakeAt = null
+      if (detail) console.warn(`CapturePack native host disconnected: ${detail}`)
       scheduleRetry()
     })
-    port.postMessage({
+    connected.postMessage({
       type: 'host.hello',
       protocol: PROTOCOL,
       timestamp: Date.now(),
       app: 'capturepack-extension',
       version: chrome.runtime.getManifest().version,
     })
-    handshakeAt = Date.now()
-    retryMs = RETRY_MIN_MS
   } catch {
     port = null
     handshakeAt = null
