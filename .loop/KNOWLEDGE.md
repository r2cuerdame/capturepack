# capturepack — KNOWLEDGE

<!-- 반복해서 필요한 저장소 구조·실행법·제약·검증된 사실. 추측은 넣지 않는다. -->
- 2026-08-08: **저장소 규모/검증 수치 (실측)**: core/src TypeScript 131개 파일 72,267줄. core/package.json 의 check:* 스크립트 82개. `npm run qa:rc` = 85 게이트 단계(--skip-build 시 83). 체크 개수는 릴리스마다 변하므로 인용 전 직접 세는 것이 저장소 규칙입니다: `node -e "const s=require('./core/package.json').scripts;console.log(Object.keys(s).filter(k=>k.startsWith('check:')).length)"` **qa-gate 실행 주의**: 반드시 `npm run qa:rc` 로 실행. `node scripts/qa-gate.mjs` 로 직접 부르면 npm_execpath 를 잃고 npm.cmd 스폰으로 폴백하는데 Node 24 는 이를 거부합니다. **docs/HANDOFF.md 가 실질적 최상위 인계 문서**입니다. 캡처·타임라인·오브젝트 피킹·다중 디스플레이·뷰어·패키징·릴리스 코드를 건드리기 전에 반드시 읽어야 하며, "Non-negotiable product boundaries"와 "Machine safety" 절은 에이전트 행동 규약입니다 — 대표의 전역 캡처 핫키나 마우스 입력 합성 금지, `%LOCALAPPDATA%\Programs\capturepack` 설치본 변경 금지, 캡처 루트 `C:\_CapturePack` 는 증거이므로 쓰기/이동/삭제 금지. **함정 1 — supervision-standdown**: 이름과 달리 감시(supervision) 기능이 아닙니다. watchdog 은 0.3.5 에서 완전히 제거되었고(#80), 이 플래그는 설치 프로그램이 실행 중인 앱을 닫기 전에 쓰고 Chrome 네이티브 호스트가 그동안 종료되도록 하는 핸드셰이크입니다. 지우는 코드는 앱 시작 경로(index.ts clearInstallerStandDown)에 있습니다. **함정 2 — grep 만으로 판단 금지**: `WH_KEYBOARD` 와 `input.key.*` 는 소스에 등장하지만 전부 "이 후크는 절대 설치하지 않는다"를 설명하는 주석입니다. 주변 줄을 읽고 결론 내야 합니다. **함정 3 — #89 보정을 되살리지 말 것**: 픽셀 노출 지연은 실재하고 측정되었으나(92~127ms 범위에서 값이 수렴하지 않음) 보정은 제품 결정으로 거부되었습니다(2026-08-01). 125ms 등 어떤 전역 상수도 하드코딩 금지. `exposureCorrectedContextTimeMs` 는 exposureAlignment.ts 내부 측정 하네스(scoreCandidate)에서만 쓰이며 제품 적용 지점은 0개입니다. **두 번째 프로세스는 회귀**: Task Manager 에 CapturePack.exe 가 둘 보이면 구현 세부가 아니라 버그입니다. **애플리케이션 버전과 팩 포맷 버전은 별개 계약**입니다. core/package.json 은 0.4.1, 팩 포맷은 0.8.0 까지 가산적으로 진화했으며 리더는 앱 버전이 아니라 SPEC.md 를 따라야 합니다. **숫자 할당 규칙은 구현이 하나여야 합니다**: N 개의 번호 박스는 정확히 1..N. 과거에 annotations 스키마·팩 생성 skills 문단·tools/validate-capturepack.mjs 안에 stale 사본 3개가 발견된 적이 있습니다. **릴리스는 push/tag 로 발행되지 않습니다** — 수동 workflow_dispatch 만이 지원 경로이며(docs/RELEASING.md), 공개 버전 덮어쓰기 금지, 핫픽스는 상위 버전으로 fix-forward. **보고는 한국어로, 측정된 결과와 함께.** 미검증 항목은 통과가 아니라 "미검증"으로 명시하는 것이 이 프로젝트의 관행입니다.
