# DOM / semantic objects

No DOM metadata in this pack.

The capture was taken in Google Chrome, but no browser plugin contributed DOM data (there is no
`plugins/` payload and no annotation carries a `target`). If a future pack includes it, DOM
metadata lives under `plugins/<name>/` and semantic object references appear as the reserved
`target` field on annotation boxes.
